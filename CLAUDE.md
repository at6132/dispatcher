@AGENTS.md

Product purpose: `docs/PRODUCT.md` and `.cursor/rules/product.mdc`.

Follow the design profile in AGENTS.md and `.cursor/rules/design-profile.mdc`. Use `src/theme` tokens and `GlassSurface` for UI chrome.
